export type RegistrationInput = {
  email: string;
  username: string;
  firstName: string;
  lastName: string;
};

export type PublicUser = {
  id: number;
  email: string;
  username: string;
  firstName: string;
  lastName: string;
};
